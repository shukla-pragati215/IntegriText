# MongoDB Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/integritext?retryWrites=true&w=majority

# JWT Secret (use a strong random string)
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production

# Node Environment
NODE_ENV=production

# Server Port (Vercel will set this automatically)
PORT=5000
